# Copy this file to `.env` and fill in your real Supabase project values
# once it exists. Until then, leave `.env` absent (or these blank) and the
# app automatically runs on the mock data in src/data/messages.js instead.

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
